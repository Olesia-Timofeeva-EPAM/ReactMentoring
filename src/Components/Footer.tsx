import * as React from 'react';

import * as styles from '../styles/footer.scss';

const Footer = () => {
    return (
        <footer className={styles.footer}>
            <span className={styles['footer__text']}>netflix</span>
        </footer>
    );
};

export default Footer;